# 핸드오프 (요청) — 현재 구현 상태 진단

> **누가 읽나:** SYOTOS 저장소에 접근 가능한 Claude Code.
> **왜:** 다른 세션(설계 논의)에서, "TWoM식 연속 전진 횡스크롤"을 의도했으나 구현이 사실상 포인트앤클릭(서울 2033 결)에 가까워졌다는 진단이 나왔다. 이걸 (A)원래 루프로 되돌릴지, (B-1)쌍둥이 게임으로 재명명할지, (B-2)포인트앤클릭을 확정하되 소모 축을 재설계할지 결정해야 한다. 그 결정을 *추측이 아니라 코드 사실* 위에서 내리기 위한 진단 요청이다.
> **작성:** 2026-06-30 / 게임 = See you on the other side (SYOTOS), Godot 4.x 2D, 웹 배포.

---

## 진단 요약 (맨 위 3줄)

> 진단 수행: 2026-07-17, 코드 직접 확인 기준. 아래 §A~§H 각 질문에 상세 답변.

1. **현재 몸:** 하이브리드다. 노드 사이 이동은 지도 위에서 마커가 실제로 걷는 **연속 자동 전진**(0.65초마다 한 걸음, 걸음마다 소모)이고, 도착한 노드 안은 정적 그림 위 클릭 지점(포인트앤클릭 결)이다. "사실상 포인트앤클릭이 됐다"는 전제는 현재 코드와 다르다.
2. **소모 축 생존:** 살아 있다. 전진하려면 반드시 엣지 걸음을 걸어야 하고, 걸음마다 물(거리 비례 증가)·식량이 강제 차감되며, 고갈사 코드 경로가 실재한다. 공짜 이동은 없다.
3. **가장 싼 길:** B-2. 단, B-2가 하려던 "소모 축 재설계"는 이미 코드에 구현돼 있어 추가 작업이 사실상 0이다(남는 건 밸런스 튜닝뿐). A는 ui 층 전면 재작성(큼), B-1은 그보다 크다.

> **전제 갱신 주의:** 이 문서(06-30 작성) 이후 3주간 코드가 크게 진화했다. "지도는 StS식 노드 그래프로 재설계 중"은 완료됐고, `docs/design/00_START_HERE.md` §9-2는 옛 횡스크롤 이동 씬 모델을 이미 "폐기"로 기록하고 있다. 즉 A/B 결정의 상당 부분이 7월 개발에서 사실상 내려진 상태다. 아래 답변은 그 현재 코드를 사실대로 기술한다.

---

## 답변 방법 (중요)

- **코드 사실과 추론을 구분할 것.** 각 답에 근거 파일/함수/변수명을 실제로 인용하라. 확인 못 한 건 "코드 미확인, 추정"이라고 명시.
- **"아마 그럴 것"으로 답하지 말 것.** 해당 스크립트를 열어 확인하고 답하라.
- **플레이 체감이 필요한 항목(§G에 표시)**은 코드로 알 수 없으므로 "이건 사용자 플레이 판단 필요"라고 넘겨라 — 대신 코드가 그 체감을 *어떻게 좌우하는지*는 설명하라.
- 각 질문 아래에 바로 답을 적어 이 문서를 채운 뒤 반환하는 형식이면 가장 좋다.

---

## §A. 이동 / 전진 모델 — (핵심: 우리가 어떤 '몸'을 가졌나)

A1. 플레이어가 공간을 이동하는 방식은 **연속**(캐릭터가 화면 안에서 실제로 가로로 걸어 이동)인가, **이산**(지점/장면을 클릭하면 그리로 점프/전환)인가? 코드로 근거를 대라.

> **답: 두 층으로 나뉜다. 노드 사이 = 연속(자동), 노드 안 = 정적.**
> - 지도에서 다음 노드를 탭하면(`Map._gui_input` → `GameState.begin_travel(hit)`, `Map.gd:519`) 마커가 그 노드까지 지도 위 곡선을 따라 실제로 이동한다. `Map._process`(`Map.gd:414~486`)가 `STEP_INTERVAL = 0.65`초(`Map.gd:9`)마다 `run.step()`을 부르고, 이동 중엔 매 프레임 다시 그려 마커가 곡선 위를 미끄러진다(`Map.gd:453`).
> - 단, 걸음 입력형이 아니라 **자동 전진**이다. 탭 1회 = 엣지 하나 전체를 걷는 계약이고, 이동 중 상황 카드가 뜨면 멈췄다가 결정 후 재개한다(`Map.gd:484~486`).
> - 도착하면 씬 전환으로 그 노드의 단면 화면에 들어가고(`GameState.go_to_expedition`, `Map.gd:471`), 그 안에서는 공간 이동이 없다. "점프/전환"이 일어나는 건 이 도착 전환뿐이며, 그 전환의 대가(걸음 소모)는 이미 치른 뒤다.

A2. 한 화면(단면/풍경) 안에서 캐릭터 스프라이트가 물리적으로 움직이나? 아니면 화면은 정적이고 클릭 가능한 상호작용 지점만 있나?

> **답: 단면 안에서는 정적 + 클릭 지점이다. 걷는 몸은 지도 위 마커다.**
> - 단면(`scenes/expedition.tscn` + `Expedition.gd`)은 화면 전체를 덮는 한 장 그림(`SectionArt.draw_section`) 위에 조사 지점 마커를 찍고, 탭 히트테스트(반경 40px)로 조사한다(`Expedition._gui_input`, `Expedition.gd:484~499` → `_probe_spot`). 캐릭터 스프라이트 이동은 없다.
> - 물리적으로 움직이는 캐릭터 표현은 지도 위 원정대 마커(+걸음마다 잉크 얼룩 `_splashes`, `Map.gd:466~468`)가 담당한다.

A3. "전진"의 최소 단위가 코드에서 무엇으로 구현돼 있나 (leg? scene? node? area?)? 그 단위 하나를 넘어가는 트리거는 무엇인가 (걸음 입력? 지점 클릭? 다음 버튼?)?

> **답: 최소 단위 = 걸음(step). 변수로는 `leg`(원정 누적)·`_edge_step`(엣지 내)이며, 상위 단위 = 엣지(노드 사이 구간).**
> - `ExpeditionRun.step()`(`ExpeditionRun.gd:353~380`)이 `leg += 1; _edge_step += 1` 후 자원을 차감한다. 엣지는 `begin_edge(target_id)`(`ExpeditionRun.gd:287`)로 시작한다.
> - 엣지의 걸음 수는 고정이 아니라 지도상 실거리 비례: `MapGraph.edge_steps()`(`MapGraph.gd:622~623`) = 곡선 경로 픽셀 길이 × `STEPS_PER_UNIT`(0.027), 2~11 걸음으로 클램프.
> - 걸음 하나를 넘기는 트리거는 입력이 아니라 **시간 타이머**(0.65초, `Map.gd:455~462`)다. 플레이어 입력 단위는 두 개: ① 지도에서 다음 노드 탭(엣지 시작) ② 단면에서 "떠난다 · 지도로" 버튼(`Expedition._on_advance` → `GameState.arrive_node`).

A4. 화면 하나에 담기는 공간의 범위는? (한 화면 = 하나의 '장소/방'인가, 아니면 스크롤되는 긴 단면의 일부인가?)

> **답: 한 화면 = 한 노드(장소). 스크롤 없음.**
> - 단면은 화면 전체 cover 한 장이다: `Expedition._draw`에서 `_section_rect = Rect2(Vector2.ZERO, rect)`(`Expedition.gd:601`). 긴 단면의 일부를 스크롤하는 구조가 아니다.
> - 지도는 세계 전체를 담되, 가본 만큼만 조여진 뷰 창(`_view`, `Map.gd:157`)이 새 노드가 열릴 때마다 넓어진다.

---

## §B. 자원 소모 모델 — (핵심: '전진의 비용'이 살아있나)

B1. 물·식량 등 자원이 **언제** 감소하나? (이동 시? 장면 전환 시? 실시간 경과? 특정 행동 시?) 감소를 유발하는 코드 위치를 인용하라.

> **답: 이동 걸음마다. 그 외엔 선택의 대가와 남기기 비용.**
> - 걸음 소모: `ExpeditionRun.step()`(`ExpeditionRun.gd:358~363`). 매 걸음 물 `-water_cost()`, `FOOD_EVERY = 2` 걸음마다 식량 -1.
> - `water_cost()`(`ExpeditionRun.gd:108~111`) = 기본 1 + 거리 누적(`leg / DESOLATION_EVERY(32)`) + 무게 초과 페널티 + 직능 보정. 멀수록 한 걸음이 비싸진다(거리 곡선).
> - 그 외 감소: 이벤트 선택 효과(`apply_choice`, `ExpeditionRun.gd:404~419`), 남기기 비용(`do_leave`, `ExpeditionRun.gd:271~273`), 단면 지점의 음수 효과. 장면 전환 자체나 실시간 경과로는 소모하지 않는다.

B2. 소모가 지금도 **"전진의 비용"**으로 작동하나 — 즉 더 나아가려면 반드시 자원을 쓰게 돼 있나? 아니면 이동이 공짜가 되고 소모가 약해졌거나 사라졌나?

> **답: 살아 있다.** 노드 사이를 잇는 유일한 경로가 `begin_edge → step` 루프라, 전진에는 반드시 걸음 수 × 소모가 든다. 걸음 수가 실거리 비례(`edge_steps`)라 "멀리 갈수록 비싸다"도 유지된다. 공짜 이동 경로는 코드에 없다(개발용 `DebugOverlay` 노드 점프 제외). 이 문서가 우려한 "이동이 공짜가 된" 상태는 현재 코드에 존재하지 않는다.

B3. 자원 고갈로 인한 사망이 실제 코드 경로로 존재하나? 그 조건은?

> **답: 존재한다.** `ExpeditionRun._check_death()`(`ExpeditionRun.gd:429~435`): 물 <= 0 이면 `death_cause = "thirst"`, 식량 <= 0 이면 `"hunger"`. `step()`과 `apply_choice()` 양쪽에서 호출되어 걸음 소모·선택 대가 모두 죽음에 닿는다. 죽으면 `Expedition._die`(`Expedition.gd:434~445`)가 BODY 흔적을 남기고 `GameState.record_death`를 부른다. 부가로 바닥 스침(임계 1)·위기 강행 시 대원이 스러지는 행렬 손실도 있다(`_check_close_call`/`_lose_mate`).

---

## §C. leg 개념의 생존 — (핵심: 거리/축적이 어떻게 측정되나)

C1. 기획서의 **leg(걸음 단위)** 개념이 코드에 아직 존재하나? 아니면 scene/node 인덱스로 대체됐나? 변수명으로 확인해 답하라.

> **답: 존재하되 역할이 좁아졌다.** `ExpeditionRun.leg`(`ExpeditionRun.gd:47`, "원정 전체 누적 걸음(거리 곡선)")가 여전히 소모 곡선(`water_cost`)·상황 스케줄(`_next_situation_leg`)·초반 안전지대(`EARLY_SAFE_LEG`)의 축이다. 다만 **공간 키는 node_id 로 대체**됐다: `TraceData.gd:27~29` 주석 "분기 맵에선 node_id 가 위치의 진실(leg 는 표시·정렬용)". `00_START_HERE.md` §9-3도 같은 내용을 명시한다. 요약하면 leg = 시간/피로의 축, node_id = 공간의 축으로 분화했다.

C2. "가장 멀리 간 지점"(도달 거리)이 어떻게 측정·저장되나? (leg 번호? scene 인덱스? 무엇이든 `_max_reached`류 변수의 실제 정체.)

> **답: `_max_reached`류 변수는 없다.** 도달 축적은 두 갈래로 저장된다: ① `GameState.visited_nodes`(`GameState.gd:27`, `_mark_visited`) 방문 노드 누적(영속) ② 파생값 `GameState._max_row_visited()`(`GameState.gd:144~148`) = 방문 노드의 최대 `row`(진행 층, 공훈 판정용). end 도달 기록은 `GameState.arrivals` 배열(`GameState.gd:37`)이 따로 담는다.

C3. 탑뷰 지도의 안개 걷힘이 이 단위와 연동돼 있나? 지도는 별도 재설계(StS식 노드 그래프) 중이라 했는데, 그 재설계가 이동 모델과 정합적인가 충돌하나?

> **답: 연동돼 있고, 재설계는 완료됐으며, 정합적이다.**
> - 안개/정체 공개는 `visited_nodes` 기준(`Map._is_revealed`, `Map.gd:353~354`), 지도 뷰 확장도 같은 축.
> - 렌더 좌표와 걸음 수 계산이 같은 `MapGraph.LAYOUT`을 쓴다(`MapGraph.gd:547~548` "Map 렌더와 공유하는 단일 진실"). 즉 지도에서 보이는 거리 = 실제 치르는 걸음 수라, 그래프 재설계와 이동 모델이 한 몸으로 붙어 있다. 충돌 없음.

---

## §D. 폭풍 타이밍 — (핵심: 위협 삼각형의 정체성 축이 뭘 기준으로 도나)

D1. 폭풍이 **무엇을 기준으로** 발생/도래하나? (실시간 타이머? 이동/전진 횟수? 장면 수? 턴 카운트?) 코드 근거.

> **답: 세 형태가 각기 다른 기준으로 존재한다. 실시간 타이머 폭풍은 없다.**
> - ① 고정 폭풍 노드 2곳: `MapGraph.NODES`의 `"kind": "storm"` = c2 모래의 벽(`MapGraph.gd:222`), f1 폭풍의 문(`MapGraph.gd:470`). 기준 = 그래프 위치(도착).
> - ② 이동 중 폭풍 상황 카드: `Situations.CATALOG`의 threat=STORM 카드가 biome 가중 랜덤(`Situations.pick`, `Situations.gd:358~`)으로, 걸음 간격 스케줄(`GAP_MIN~GAP_MAX` = 2~4걸음, `ExpeditionRun.gd:18~19`)에 실려 나온다. 기준 = 걸음 수 + 확률.
> - ③ 원정 사이 폭풍 막간(Interlude): 런 종료(죽음/순환)마다 반드시 1회(`GameState.go_to_interlude`, `GameState.gd:427~442`). 기준 = 원정 횟수. 폭풍이 세계의 시계라는 서사를 연출로 물리화한 것.

D2. 폭풍이 여전히 **위치 강제**(은신처로 피해야 살아남음)로 작동하나, 아니면 다른 형태로 바뀌었나?

> **답: 위치 강제의 변형으로 유지된다. 단, 방향이 뒤집혔다: 폭풍이 나를 쫓아오는 게 아니라 내가 폭풍의 자리로 간다.**
> - 폭풍 노드의 주요 지점은 필수 위협이라 마주하기 전엔 못 떠난다: `SectionRun._is_mandatory_threat`(`SectionRun.gd:207~211`, STORM/BLOCKAGE만) + `Expedition._on_advance` 게이트(`Expedition.gd:283`) + 안내문 "폭풍을 지나기 전엔 떠날 수 없다"(`Expedition.gd:663~664`).
> - 대비 자원은 장막(shelter)이다(`Threats.INFO`, `Threats.gd:32~38`).
> - 옛 설계의 "폭풍이 시간 맞춰 도래하고 은신처 위치로 피신" 같은 타이밍 추격형 메커닉은 코드에 없다. Interlude 도 순수 연출이라 흔적·안개를 실제로 깎지 않는다(`GameState.gd:433` 주석 + `Interlude.gd`에 상태 변경 코드 없음을 확인).

D3. 폭풍 타이밍이 §A의 전진 단위와 연동돼 있나? (예: "N leg마다" vs "N scene마다" vs 실시간.)

> **답: 연동돼 있되 "N마다" 같은 주기 규칙은 없다.** ①은 노드 단위(고정 위치), ②는 걸음 단위 스케줄에 편승하는 확률(별도로 엣지당 위기 순간 확률 = `CRISIS_BASE + CRISIS_RAMP × 진행도`, `ExpeditionRun._schedule_crisis`, `ExpeditionRun.gd:296~306`), ③은 원정 단위. 전부 §A의 단위(걸음/노드/원정)에 걸려 있고 실시간과는 무관하다.

---

## §E. 남기기 / 줍기 코어 루프 — (핵심: 게임의 심장이 실제로 도나)

E1. "남기기"(런당 1회, 물건 하나 두기)가 지금 **어떤 자리 단위**에 묶이나 (leg? scene?)? 남긴 위치가 어떻게 저장되나?

> **답: node_id 단위 + 이동 중이면 엣지 위 좌표 보정.**
> - `BequeathPanel._commit`(`BequeathPanel.gd:411~445`): `trace.node_id` = 도착 노드(단면에서 남김) 또는 떠나온 노드(지도 이동 중 남김). 이동 중이면 `to_node` + `position`(= `edge_fraction()`, 엣지 진행률 0~1)으로 엣지 위 실제 지점까지 찍는다.
> - 저장은 `GameState.traces` 배열(`TraceData.to_dict()`의 JSON, `GameState.leave_trace` → `save_game`). 런당 1회는 `ExpeditionRun.bequeathed` 토큰(`ExpeditionRun.gd:51`, `can_leave`가 게이트).

E2. 다음 원정이 그 물건을 **줍는** 것이 실제로 구현·작동하나? (코드 완료 여부 + 플레이로 확인됐는지.)

> **답: 코드 완료, 전 구간 배선됨.** 경로: `GameState.pickup_traces_by_node()`(`GameState.gd:553~570`) → 새 런 생성 시 주입(`GameState.gd:320`) → 그 노드 도착 시 `arrival_event()` 우선순위 ③(`ExpeditionRun.gd:387~395`) → 줍기 카드 "집는다 / 남겨둔다"(`Situations.pickup_trace`, `Situations.gd:492~532`) → 집으면 `GameState.use_trace`가 uses 차감·0이면 제거(`GameState.gd:591~604`). 엣지 위 흔적은 이동 중 그 지점을 지날 때 자동 제안된다(`Map._edge_pickup_here`, `Map.gd:703~718`).
> - 회귀 테스트 `tests/core_smoke.gd`가 `arrival_event` 우선순위를 커버한다(`00_START_HERE.md` §8). 플레이 확인: 세션 로그·밸런스 기록 기준 폰 플레이테스트가 반복돼 왔으나, "실플레이로 봤다"의 최종 확언은 사용자 몫.

E3. **self-async** — 과거 런의 흔적이 실제로 다음 런에 나타나나? 세이브→로드 경로가 작동하나?

> **답: 작동한다.** `save_game`/`load_game`(`GameState.gd:644~715`)이 traces·deaths·flags·visited_nodes 등을 `user://otherside_save.json`으로 왕복한다(웹 = IndexedDB). 첫 세계에는 유령 흔적·유령 낙오자를 시딩해 빈 세계를 회피한다(`_plant_seeds`, `GameState.gd:74~91`). 진행 중 런의 서스펜드 저장(SAVE_VERSION 2, 걸음·선택마다 autosave)까지 있다.

E4. 남기기 비용(자원을 그만큼 잃음) 트레이드오프가 코드에 살아있나?

> **답: 살아 있고, 줍기 보상과 대칭이다.**
> - `LEAVE_COST`(`ExpeditionRun.gd:32`) = 물 4·식량 3·장막 1 등. `do_leave`가 즉시 차감(`ExpeditionRun.gd:271~273`), `can_leave`는 "남기고도 물/식량 1 이상"을 강제한다(`ExpeditionRun.gd:266~267`, 남기다 죽는 일은 없게).
> - 줍기 보충량(`Situations.pickup_trace`: 물 4·식량 3·장막 1)과 정확히 대칭. "내가 잃은 만큼이 다음 원정대의 몫"이라는 주석도 양쪽에 있다.
> - 예외 하나: 추모 표식(`do_leave_mark`, `ExpeditionRun.gd:275~281`)은 자원 무료지만 런당 1회의 남기기 토큰을 소진한다(물건 대신 기림에 쓰는 트레이드오프).

---

## §F. 흔적을 만나는 감각 — (핵심: '읽기'인가 '물질'인가)

F1. 흔적/물건을 만날 때 플레이어에게 **어떻게 표시**되나? (클릭→텍스트 팝업? 화면 속 물건 아이콘? 자동 조우?)

> **답: 세 채널이 있다.**
> - ① 도착 단면: 흔적이 그 노드의 주요 지점 마커로 그림 위에 찍히고(라벨은 일반 문구, `SectionRun._main_label` 폴백 "살펴본다"), 탭하면 카드 모달이 뜬다: "이전 원정대가 남긴 물통. 아직 쓸 만하다. 곁의 표식: [ 여기 · 안전 ]" + 집는다/남겨둔다(`Situations.gd:523~532`).
> - ② 지도: 노드 옆 흔적 아이콘(자원별 안료색 점), 탭/호버하면 표식 단어 두루마리가 펼쳐진다(`Map._active_trace`/`_trace_open_t`, `Map.gd:140~142`).
> - ③ 엣지 위: 이동 중 그 지점을 지나면 자동으로 줍기 카드가 뜬다(`Map._process` → `_edge_pickup_here`, `Map.gd:479~483`). 유일한 자동 조우.

F2. 지금 조우 경험이 **"메시지를 읽고 해석하는"** 느낌에 가깝나, **"물질(물건)을 발견하는"** 느낌에 가깝나? (F1의 구현이 이걸 어느 쪽으로 미는지 설명하라. 이건 두 게임의 정서 심장 배정에 직결된다 — 읽기=신뢰/거짓 축, 물질=예측/희생 축.)

> **답: 구현은 명확히 "물질 발견" 쪽으로 민다.** 근거:
> - 조우의 1차 표면이 물건 이름(물통·식량 자루·장막)과 자원 수치이고, 태그는 물건 "곁의 표식"으로 붙는 부속이다. 해석할 메시지가 주인공이 아니라 쓸 물건이 주인공.
> - 해석·의심의 축은 코드에 의도적으로 없다: 시딩 주석이 "정직한 표식만(오인·거짓 없음 — 신뢰는 우리 게임 주축 아님)"이라고 명시한다(`GameState.gd:72`).
> - uses 유한(나눠 쓰다 소진), 집기/남겨두기 선택, 남기기 비용과 줍기 보상의 대칭까지, 정서 배선 전체가 예측/희생 축이다. 읽기=신뢰/거짓 축은 이 빌드에 없으므로, 그 축이 필요하다면 쌍둥이 게임의 몫으로 보는 게 코드 사실과 맞는다.

F3. 유한 단어 풀 태그가 물건 위에 실제로 얹혀 표시되나? 어떻게?

> **답: 표시된다. 단, 물건 그림 위 상시 오버레이는 아니고 세 자리에서 문자로 나온다.**
> - 줍기 카드 텍스트 줄 "곁의 표식: [ 단어 · 단어 ]"(`Situations.gd:520~522`).
> - 지도 흔적 아이콘의 표식 단어 펼침(위 F1-②, 낡은 붉은 잉크색 `TRACE_MARK_INK`, `Map.gd:113`).
> - 죽음 화면 "남긴 것: 시체 [ 갈증 · 끝 ]"(`Expedition.gd:470~472`).
> - 태그 선택은 `BequeathPanel`에서 `WordPool`(27단어) 중 최대 2개.

---

## §G. 서사 정합성 & 체감 — (사용자 플레이 판단 필요 항목)

> 이 절은 코드로 단정할 수 없다. Claude Code는 "코드가 이 체감을 어떻게 좌우하는지"만 설명하고, 최종 판단은 사용자에게 넘겨라.

G1. 모래폭풍·원정대·순환 결말이라는 서사가 지금의 몸(현재 이동/조우 구조)에 **자연스럽게 붙나**, 아니면 붕 떠 보이나? (코드 관점에서 근거가 될 만한 지점이 있으면 지적.)

> **답: 최종 판단은 사용자 몫. 코드 관점의 근거만 짚는다.**
> - 붙는 지점: 폭풍=시계가 Interlude 로 물리화됐고(런 종료마다 세계를 쓸고 지나감), "말은 못 남기고 물건만"은 자유 텍스트 입력이 아예 없고 WordPool 태그만 있는 구조로 강제된다. 순환/재회 결말도 구현 완료(`GameState.ending_kind`, `Expedition._show_ending`).
> - 뜨는 지점 하나: "모래폭풍이 글씨를 지운다"는 법칙과 달리 흔적이 시간에 침식되지 않는다. uses 소진만 있고 Interlude 는 순수 연출이다(`GameState.gd:433` 주석 "흔적 uses·안개를 실제로 깎지 않는다. 시간 기반 흔적 소멸은 별도 결정 후 후속"). 서사와 시스템 사이의 이 틈은 코드 주석 스스로 미결로 기록하고 있다.

G2. (사용자용) 지금 플레이해보면 재밌나? 원래의 TWoM 연속 전진 루프가 아쉬운가, 아니면 지금 형태가 더 나은가?

> **답: 사용자 플레이 판단 필요.** 코드가 좌우하는 부분만: 걸음의 감각은 0.65초 간격 자동 전진 + 발소리 + 잉크 얼룩으로 표현되고, 결정 밀도는 걸음 입력이 아니라 2~4걸음마다의 카드와 노드당 조사 예산 2로 만들어진다. "한 걸음마다 결정"이 아니라 "한 구간마다 몇 번의 결정"인 페이싱이다.

---

## §H. 스코프 / 현실 — (핵심: 각 선택지의 실제 작업량)

> A/B-1/B-2 각각을 실행할 때 이미 짠 코드를 얼마나 살리고 얼마나 버리는지가 결정의 무게추다.

H1. **(A로 갈 때)** 지금 상태에서 "연속 이동 + 걸음마다 소모" 루프로 되돌리려면 어떤 작업이 필요한가? 대략의 규모(작음/중간/큼)와 버려질 코드/살릴 코드를 구분하라.

> **답: 규모 큼(ui 층 전면). 단, core 는 이미 걸음 모델이라 생각보다 많이 산다.**
> - 살릴 것: `scripts/core` 전부. 특히 `ExpeditionRun`은 파일 헤더부터 "엣지 단위로 횡스크롤 전진한다"(`ExpeditionRun.gd:4`)로 짜여 있어 `step`/`leg`/`begin_edge`/`edge_fraction`이 그대로 횡스크롤 씬의 구동부가 된다. `GameState`·세이브·이벤트 콘텐츠(`MapGraph.NODES.events`·`Situations.CATALOG`)도 산다.
> - 버릴/새로 짤 것: 걷는 캐릭터·패럴랙스 배경의 횡스크롤 씬(신규 제작), `Map.gd`의 마커 이동 연출부(지도는 계획 화면으로 강등), `SectionRun`/`SectionArt`의 단면 지점 탐색(폐기 또는 도착 이벤트로 축소), 노드별 spots 좌표 콘텐츠 상당수.
> - 가장 아픈 손실은 코드보다 에셋이다: 노드 맞춤 단면 그림 8종(01~79 아트 체계)이 지점 탐색 전제로 발주·정렬돼 있어 용도가 준다.

H2. **(B-2로 갈 때)** 지금 포인트앤클릭 구조를 **유지**하되, 소모를 이동이 아니라 다른 축(예: scene 전환마다 소모, 폭풍을 scene 카운트로)으로 다시 거는 작업량은? 무엇을 바꿔야 하나?

> **답: 사실상 0. 이 재설계는 이미 구현돼 있다.** 소모는 scene 전환이 아니라 "노드 선택이 계약하는 엣지 걸음"에 걸려 있고(§B), 그 걸음 수가 실거리 비례라 "전환마다 소모"보다 정밀한 축이다. 폭풍도 노드/걸음/원정 세 축에 이미 걸려 있다(§D). 남는 작업은 밸런스 튜닝뿐이며, 그 파이프라인(`tests/balance_sim.gd`, 기준선 end 19.0%)도 이미 돌아간다.

H3. **(B-1로 갈 때)** 지금 빌드를 '쌍둥이 게임'으로 떼어내고 SYOTOS를 연속 전진으로 새로 시작한다면 — 지금 코드 중 쌍둥이로 그대로 가져갈 수 있는 부분과, SYOTOS 재시작 시 재활용 가능한 부분을 각각 나눠라.

> **답:**
> - 쌍둥이로 그대로: 현 빌드 전체(그 자체로 완결 루프 + 오프닝/엔딩/오디오/아트까지 붙어 있다).
> - SYOTOS 재시작 시 재활용: `ExpeditionRun`(걸음 모델 그대로), `TraceData`/`Threats`/`WordPool`/`Feats`/`Vocations`(범용 데이터), `GameState`의 세이브/라우팅 뼈대, `UITheme`/`AudioManager`/`StormFX`/`Transition` 인프라, 웹 export 파이프라인.
> - 재활용 어려운 것: 콘텐츠 전부. `MapGraph` 9노드 이벤트·spots, 아트 01~79, 오디오 33종이 현 빌드의 지도/단면 구조에 종속돼 있어 이식 손실이 가장 크다.

H4. 위 셋 중, **순수하게 코드 재활용 관점에서만** 가장 적은 낭비로 가는 길은 무엇인가? (정서·설계는 빼고 오직 구현 경제성만.)

> **답: B-2.** 재활용 100%, 신규 0, 낭비 0. A 는 core 재활용률은 높지만(체감 70% 이상) ui·아트·콘텐츠 좌표의 손실과 신규 씬 제작이 크다. B-1 은 SYOTOS 몫을 처음부터 다시 짓는 것이라 낭비가 가장 크다. 덧붙여 B-2 의 전제 조건이었던 "소모 축 재설계"가 이미 끝나 있으므로, 코드 경제성 기준의 답은 "지금 몸을 정본으로 확정"이다.

---

## 반환 시 요약 (맨 위에 3줄로)

문서를 채운 뒤, 맨 위에 이 세 줄만 뽑아 요약해 달라:
1. **현재 몸:** 연속 전진인가 / 포인트앤클릭인가 (한 줄 판정). → 채움(문서 상단 "진단 요약" 참조).
2. **소모 축 생존:** '전진의 비용'이 살아있나 / 죽었나. → 살아 있음.
3. **가장 싼 길:** 코드 경제성만으로 A/B-1/B-2 중 무엇. → B-2 (추가 작업 사실상 0).
