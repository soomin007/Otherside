# Backlog — 앞으로 할 일 (다음 작업의 단일 소스)

> 합의됐으나 미착수인 작업만 남긴다. 완료·폐기 항목은 지운다(✅ 목록을 쌓지 않는다).
> 무엇을 왜 했는지는 `session_logs/` 에. **현재 상태·전체 맥락은 `00_START_HERE.md`** (새 세션은 그것부터).

## 데스크톱 (구현 — 귀한 시간)

- [ ] **업적/도전과제 시스템 (2026-07-11 사용자).** 설계부터 시작. 후보 과제: 첫 도달·재회·무손실 완주·전 노드 방문·
      낙오자 전원 구조 등. 표시는 원정 일지의 새 챕터 후보(시스템 말 "도전과제" 대신 세계의 말로 — 마을의 벽서·공훈록 등).
      **직능(대장 특기) 선택을 업적 보상으로 해금하는 안(사용자 제안, 2026-07-11)과 묶어 설계** — 첫 원정은 평범한 대장 고정,
      과제를 이루면 마을에 새 인물이 온다(예: 갈증사 겪기 → 물지기, 무거운 가방 완주 → 짐꾼). 첫 화면의 인지 부하를 줄이고
      해금 자체가 온보딩이 된다. 조건·보상 표는 사용자와 확정 후 기획서 반영.

- [ ] **공략/매뉴얼 리뉴얼 (2026-07-11 사용자).** 재회 축 개편(2026-07-10, 옛 "흔적 4개" 폐기 → 기림 2·구조 2·온전)으로
      기존 외부 공략(HTML)이 무효. 세 축 기준으로 재발행 + 일지 "조작" 챕터(안내 4장·표식 읽기)와 시장 대사가 현행 규칙과
      어긋난 곳 없는지 대조. 무게·행렬·낙오자·서스펜드 저장 등 이후 추가된 시스템도 실어야 한다.

- [ ] **분위기 시스템 (2026-07-05 사용자).**
      (a) 연출 속도 설정 — 2026-07-07 완료(설정 "연출" 슬라이더, `AppSettings.motion` → Transition 전환·등장·Bookmark 책넘김. 세기 0=즉시). 남은 것: 지도 드리프트·폭풍 파티클도 같은 값 반영(원하면).
      (c) 지도 전환 ui_scatter 태깅 — 지도 좌/우 칼럼이 `_draw` 라 현재 통짜 폴백.
      ((b) 위치 반영 환경 강도는 2026-07-06 완료 — 지도 드리프트 + 바람 돌풍 환경음, `MapGraph.progress` 공용. 실플레이 체감 확인만 남음.)

- [ ] **가로 고정(landscape) 실기기 확인.** 구현 완료(2026-07-05, `project.godot` orientation=sensor_landscape + 세로 안내 `Fullscreen.gd`, 웹 전용). 남은 것: 안드로이드(첫 탭→전체화면+가로 잠금)·iOS(잠금 불가→안내→물리 회전)·회전 시 레이아웃 실기기 확인.

- [ ] **글씨 흐림/깨짐 — 배포 후 재확인.** 1차 조치(`default_texture_filter` Nearest→Linear). 배포 후 폰/시크릿 창에서 또렷한지 확인. 여전하면 다음 레버 = 폰트 MSDF 임포트(웹/GL Compatibility 실기기 검증 필수).

- [ ] **밸런스 튜닝 (1차 완료 — 폰 정성 확인 대기, 상세 `balance_notes.md`).**
      남은 것: (1) 폰 실플레이 정성 확인(긴장·페이싱 — 시뮬 end 도달 ~12%는 실기기서 더 낮을 수 있음), (2) 배고픔 33% 재검토(갈증/배고픔 비율), (3) 재회 임계 `REUNION_TRACES` 8→4 낮춤(2026-07-09, 죽음/시체는 카운트 0) → 폰 체감 확인, (4) 위기·무게·직능 트레이드오프 통합 완주율. (엣지 길이 거리 비례화는 2026-07-06 완료 — `MapGraph.STEPS_PER_UNIT`.)
      도구: `tests/balance_sim.gd`(손잡이 static var 스윕)·`Debug` 오버레이. 워크플로: 손잡이 → `-s balance_sim` → `core_smoke` 회귀 → 코드·기획서 동시 갱신.

- [ ] **콘텐츠 확충 (시스템 완비 — 값만 추가).** 이벤트 풀·변형·플래그 체인(`sets`/`sets_persist`/`requires`) 확충(2026-07-06 배치1: 이동 상황 `CATALOG` +5 = rock 3·정서 2, self-async 체인 +3 = c1 오아시스·c2 모래벽·오아시스→폭풍의 문 물 비축), 노드별 단면 지점(`spots`) 다양화(2026-07-07: 9노드 전부 2~3 → 4~6개, 트레이드오프 event·물↔식량 교환·자기비동기 투자·정서 empty 혼합, 순자원 ≤0 유지로 밸런스 중립), 분기 다양화(엣지 길이는 2026-07-06 거리 비례로 완료), 노드 arrival `events` 풀·threat source 다양화(2026-07-08 배치2: 도착 이벤트 +5 = a1·b1·d1×2·e1, 이동 중 차단 `rope_gully`·폭풍 `grit_wind`, `river_dug` 체인 소비처 +1. 상세 `balance_notes.md`). 남은 것: 폰 체감 후 추가 확충(어느 노드가 얇게 느껴지나), 재방문 변형(영속 플래그 체인) 커버리지는 다중 원정 시뮬(2단계) 대기. 홈 = `scripts/core/MapGraph.gd`·`Situations.gd`·`wordpool_v0.1.md`.

- [ ] **남김 밸런스 튜닝.** 본구현 완료(런당 1회 남기기·죽음과 분리·비용=줍기 값 대칭 물4·식3·은1·로프1·도구1). 도구 유품 연동 완료(2026-07-08 — 약초/부싯돌/정화천 남기기·줍기, `TraceData.is_pickable`/`kind_to_key` 일원화). 남은 것: 비용·줍기 값·uses 수치 감각(폰 테스트).

- [ ] **단면 맞춤 아트 (트랙 B).** kind별 `SectionArt` 절차적 단면을 맞춤 아트로 교체. 현재 전부 절차적 `draw_*` 1차. (실제 그림 에셋 준비되면.)

- [ ] **⑥ 결말 심화 (기획서 §3).** 엔딩 슬라이드쇼 구현됨(`Ending.gd`, 순환/재회 47~52 아트+음악). 남은 것: (a) **순환의 물리적 반영** — 도달점이 다음 원정의 새 목표가 되는 맵/그래프 변화(현재 서사만, 설계 큼), (b) 재회 세 축(기림 2·구조 2·온전) 폰 체감 확인, (c) **온전·구조 후속** — 행렬 시각화 강화 후보(실루엣 표시·손실 연출), 낙오자 이벤트 살 붙이기(원정별 이름/한 마디·거둔 이가 걷는 모습, 지금은 카드 1종), 원하면 가벼운 이빨(대원 손실 시 `WEIGHT_FREE` 소폭 감소 — 짐꾼이 줄었다) 검토. (온전 도달률 시뮬 지표는 2026-07-11 구현 — `balance_sim` `[8]`, 수치·해석은 `balance_notes.md` §다음 4번.)

- [ ] **비주얼 다듬기.** 팔레트·타이포 미세 조정, 지도 노드 칩 스타일·긴 그래프 카메라/스크롤·이름 겹침 정리. 실제 아트 01~52 삽입 완료. (자원 점 마커 스타일·범례는 2026-07-06 완료 — session_logs 참고.)

- [ ] **폭풍 3층 연출 — 실기기 성능·감각 확인만 (배선 완료 2026-07-07).** 폭풍 biome 단면(c2 모래의 벽·f1 폭풍의 문)에 3층 얹음: 1층 옅은 모래 헤이즈(`Expedition._draw`)·2층 중경 발광 먼지·3층 전경 빛나는 모래 알갱이(`StormFX`, CPUParticles2D만). 밝은 폭풍 아트 위 대비는 어두운 영역에서 읽히게 톤 조정(세션 7 교훈). 이동 중 폭풍 상황 카드(Map)에도 같은 3층 얹음(2026-07-07, 카드 dim 위·내용 아래 — 어두운 배경이라 대비 잘 삼). 남은 것: 데스크톱+모바일 성능 확인, 실기기서 움직임·밀도 감각(정지컷은 움직임을 못 담음). 셰이더/GPUParticles 금지 유지.

- [ ] **설정 확장.** 설정 = 원정 일지 "설정" 챕터로 통합됨(`Bookmark.gd` — 옛 SettingsPanel 폐지). 소리·화면·오프닝 다시보기·여정·데이터 초기화 완료. 2026-07-07 확충: 조작 챕터 "표식 읽기" 범례(지도·단면 기호 안내), 만든 것들(폰트·음악·소리·엔진 출처), 조작 안내 다시보기, 연출 세기 슬라이더. 남은 것: 접근성 글자 크기 옵션(전 레이아웃 영향 — 별도 작업). **크레딧 출처 문구(특히 소리 = freesound·ElevenLabs) 사용자 확인 필요.**

- [ ] **오프닝·가방 마감.** 오프닝 슬라이드쇼(`Opening.gd`)·시장 NPC 튜토리얼·가방 꾸리기(직능·도구·무게) MVP 완료. 남은 것: 오프닝 문구 다듬기(사용자: 나중에), 서사·NPC 실제 삽화 에셋, 원정대 이름 풀 확장, 가방 물품·도구·위기 종류 확장.

- [ ] **온보딩 실기기 미세조정.** 핵심 완료(문구·HUD·상시 책갈피·조작 오버레이 튜토리얼·원정대 이름). 남은 것: 오버레이 앵커 영역·blind choice 시각 실기기 확인.

- [ ] **실플레이 확인 (사용자 몫 — 배포/폰).** 지도 새 좌표계·책장 넘김 감각(웹에서 캡처 히치 여부)·설정 여정 흐름·씬 등장 stagger 체감·엔딩 전체 흐름(암전·프롬프트·크레딧 롤·음악).

## 오디오 (배선까지 완료 — 미세 조정만, 상세 `docs/external/audio_list.md`)

- [ ] **오디오 미세 조정.** BGM 4곡(ogg 96k, 원본 `assets_src/bgm_original/`)·SFX 29종 전부 제작·배선 완료(공통 탭·걸음 3변주·위협별 카드·호버 원 그리기 6변주·엔딩·장부 등). Music/SFX 버스·설정 볼륨 2 완료.
      남은 것: (1) 실플레이 소리 밸런스(개별 vol_db, 탭 과하면 meta `no_tap`), (2) 보강 3종(`sfx_resource`·`reveal`·`cycle`) 노이즈 확인 → 거슬리면 재생성, (3) `sfx_settings` 보류 — 어울리는 자리 생기면 배선.

- [ ] **웹 BGM 폰 타닥 — 앱 출시 때 근본 해결(사용자 확정 2026-07-06).** 원인 = Godot 웹 Threads OFF 메인스레드 믹서 글리치(폰 CPU 성능 의존, PC 웹·네이티브는 깨끗). 상세 `known_issues`. **현 상태:** 웹은 베드곡 SAMPLE 우회로 완화(연속→가끔), 개발 편의상 Pages 자동배포 유지. **네이티브 앱은 이미 `OS.has_feature("web")` 게이트로 원래 스트림 재생 = 타닥 없음.** 앱 빌드 시 할 일: 그대로도 원본 품질(스트림), 원하면 96k ogg → 원본 mp3(`assets_src/bgm_original/` 182k) 탑재. (웹의 "가끔"까지 없애려면 COOP/COEP 보내는 호스트로 이전해 threads ON — 출시 임박 카드.)

## 외부 / 배포 대기

- [ ] 웹 배포 후 폰/데스크탑 브라우저 플레이테스트 — 페이싱·밸런스 체감 + UI/폰트/전체화면 시각 확인. (배포됨: GitHub Pages 워크플로 `deploy.yml`. **다중 push 배포 큐 경합 주의 → 몰아서 push**, `known_issues` 참고.)

## 밖에서(모바일 채팅) 구체화 — 머리 쓰는 일

- [ ] 위협별 세부 설계: 차단·폭풍은 본구현됨(기획서 §4.1). 남은 것 — 소모(갈증) 단서를 더 또렷이, 죽는 순간 연출, 위협별 흔적 대비 효과 다양화.
- [ ] 자원=수명 트레이드오프 수치 감각 (무엇을 남기면 얼마를 잃나).
- [ ] **흔적 표식 판독 심화 (심장 — 2026-07-07 1차 구현).** 지도에 남긴 물건이 아이콘으로 뜨고(방문+다음 노드) 호버/탭하면 표식 단어가 펼쳐진다(`Map._draw_traces`/`_trace_at`). 유령 흔적 세계당 1회 심기(`GameState._plant_seeds`, 정직·재회 제외). 이동 중 남긴 것은 엣지 위 실제 지점에 찍힘(2026-07-07: `TraceData.to_node`/`position`, `_edge_point` 렌더 — 이동 중 시체는 쓰러진 자리에; 경로에 남긴 자원은 취약해 uses -1). 엣지 위 자원 흔적은 **지나며 줍기** 완료(2026-07-07: `edge_pickup_traces`/`use_trace_edge`, 이동 중 그 지점을 지나면 상황 카드 흐름으로 집기/남기기). 표식 우선순위/정리 완료(2026-07-08: 노드당 흔적을 우선순위 정렬 — 다리 > 아직 줄 수 있는 자원/도구 > 시체·소진, 같은 층은 최근 먼저 — 최대 `MAX_TRACE_MARKERS`(4)개만 보이고 넘치면 "+K" 요약. `Map._draw_traces`/`_trace_sort`/`_trace_priority`). 남은 것: (b) "폭풍이 세계를 조금 바꿈"(환경발 변주 — 거짓/오인 아님, 사용자 확정), (c) 유령 흔적 문구·배치 확장.
