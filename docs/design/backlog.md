# Backlog — 앞으로 할 일 (다음 작업의 단일 소스)

> 합의됐으나 미착수인 작업만 남긴다. 완료·폐기 항목은 지운다(✅ 목록을 쌓지 않는다).
> 무엇을 왜 했는지는 `session_logs/` 에. **현재 상태·전체 맥락은 `00_START_HERE.md`** (새 세션은 그것부터).

## 데스크톱 (구현 — 귀한 시간)

- [ ] **분위기 시스템 (2026-07-05 사용자).**
      (a) **연출 속도 설정** — 흩어짐·비행 등 애니 속도를 설정에서 조절(당장은 아님, 사용자 명시. 설정 확장 (a)와 합류).
      (b) **위치 반영 환경 강도 확장** — 남기기 화면 모래 드리프트는 현 위치(row 진행도)로 강도 반영됨. 같은 원리를 지도 화면 + **바람/폭풍 환경음**(후반일수록 세게)으로 확장. 오디오 배선 완료로 착수 가능(← 추천). ⚠️ 환경음은 AudioManager 조율.
      (c) 지도 전환 ui_scatter 태깅 — 지도 좌/우 칼럼이 `_draw` 라 현재 통짜 폴백.

- [ ] **가로 고정(landscape) 실기기 확인.** 구현 완료(2026-07-05, `project.godot` orientation=sensor_landscape + 세로 안내 `Fullscreen.gd`, 웹 전용). 남은 것: 안드로이드(첫 탭→전체화면+가로 잠금)·iOS(잠금 불가→안내→물리 회전)·회전 시 레이아웃 실기기 확인.

- [ ] **글씨 흐림/깨짐 — 배포 후 재확인.** 1차 조치(`default_texture_filter` Nearest→Linear). 배포 후 폰/시크릿 창에서 또렷한지 확인. 여전하면 다음 레버 = 폰트 MSDF 임포트(웹/GL Compatibility 실기기 검증 필수).

- [ ] **밸런스 튜닝 (1차 완료 — 폰 정성 확인 대기, 상세 `balance_notes.md`).**
      남은 것: (1) 폰 실플레이 정성 확인(긴장·페이싱 — 시뮬 end 도달 ~10%는 실기기서 더 낮을 수 있음), (2) 배고픔 33% 재검토(갈증/배고픔 비율), (3) 재회 임계 `REUNION_TRACES`(8) 튜닝(다중 원정 시뮬), (4) `EDGE_LEN` 엣지별 다양화, (5) 위기·무게·직능 트레이드오프 통합 완주율.
      도구: `tests/balance_sim.gd`(손잡이 static var 스윕)·`Debug` 오버레이. 워크플로: 손잡이 → `-s balance_sim` → `core_smoke` 회귀 → 코드·기획서 동시 갱신.

- [ ] **콘텐츠 확충 (시스템 완비 — 값만 추가).** 이벤트 풀·변형·플래그 체인(`sets`/`sets_persist`/`requires`) 확충(현재 `CATALOG` 10종 + `requires` 연쇄 2종), 노드별 단면 지점(`spots`, 현재 9노드×2) 다양화·event/threat source 다양화, 엣지 길이/분기 다양화. 홈 = `scripts/core/MapGraph.gd`·`Situations.gd`·`wordpool_v0.1.md`.

- [ ] **남김 밸런스 튜닝.** 본구현 완료(런당 1회 남기기·죽음과 분리·비용=줍기 값 대칭 물4·식3·은1·로프1). 남은 것: 비용·줍기 값·uses 수치 감각(폰 테스트), **도구 유품 연동**(`TraceData` 확장 — 도구 남기기/줍기).

- [ ] **단면 맞춤 아트 (트랙 B).** kind별 `SectionArt` 절차적 단면을 맞춤 아트로 교체. 현재 전부 절차적 `draw_*` 1차. (실제 그림 에셋 준비되면.)

- [ ] **⑥ 결말 심화 (기획서 §3).** 엔딩 슬라이드쇼 구현됨(`Ending.gd`, 순환/재회 47~52 아트+음악). 남은 것: (a) **순환의 물리적 반영** — 도달점이 다음 원정의 새 목표가 되는 맵/그래프 변화(현재 서사만, 설계 큼), (b) 재회 임계 `REUNION_TRACES` 밸런싱(폰 테스트).

- [ ] **비주얼 다듬기.** 팔레트·타이포 미세 조정, 지도 노드 칩 스타일·긴 그래프 카메라/스크롤·이름 겹침 정리, 자원 점 마커 스타일·범례(세션 4에서 사용자 결정 대기로 넘김 — trace-legend 세션 진행 중일 수 있음). 실제 아트 01~52 삽입 완료.

- [ ] **폭풍 CPUParticles2D 3층 연출 + 성능 테스트.** 메커니즘 본구현됨(구간형+예고, 은신처/강행). 1층(반투명 그라데이션 띠)은 `Expedition._draw_storm`. 남은 것: 2층(중경 스프라이트 몇 장) + 3층(전경 CPUParticles2D 소량), 데스크톱+모바일 성능 확인. 셰이더/GPUParticles 금지.

- [ ] **설정 확장.** 설정 = 원정 일지 "설정" 챕터로 통합됨(`Bookmark.gd` — 옛 SettingsPanel 폐지). 소리·화면·오프닝 다시보기·여정·데이터 초기화 완료. 남은 것: 접근성 옵션(글자 크기 등), 연출 속도 설정(분위기 시스템 (a)와 합류).

- [ ] **오프닝·가방 마감.** 오프닝 슬라이드쇼(`Opening.gd`)·시장 NPC 튜토리얼·가방 꾸리기(직능·도구·무게) MVP 완료. 남은 것: 오프닝 문구 다듬기(사용자: 나중에), 서사·NPC 실제 삽화 에셋, 원정대 이름 풀 확장, 가방 물품·도구·위기 종류 확장.

- [ ] **온보딩 실기기 미세조정.** 핵심 완료(문구·HUD·상시 책갈피·조작 오버레이 튜토리얼·원정대 이름). 남은 것: 오버레이 앵커 영역·blind choice 시각 실기기 확인.

- [ ] **실플레이 확인 (사용자 몫 — 배포/폰).** 지도 새 좌표계·책장 넘김 감각(웹에서 캡처 히치 여부)·설정 여정 흐름·씬 등장 stagger 체감·엔딩 전체 흐름(암전·프롬프트·크레딧 롤·음악).

## 오디오 (배선까지 완료 — 미세 조정만, 상세 `docs/external/audio_list.md`)

- [ ] **오디오 미세 조정.** BGM 4곡(ogg 96k, 원본 `assets_src/bgm_original/`)·SFX 29종 전부 제작·배선 완료(공통 탭·걸음 3변주·위협별 카드·호버 원 그리기 6변주·엔딩·장부 등). Music/SFX 버스·설정 볼륨 2 완료.
      남은 것: (1) 실플레이 소리 밸런스(개별 vol_db, 탭 과하면 meta `no_tap`), (2) 보강 3종(`sfx_resource`·`reveal`·`cycle`) 노이즈 확인 → 거슬리면 재생성, (3) `sfx_settings` 보류 — 어울리는 자리 생기면 배선, (4) **웹 오디오 확인**(브라우저 자동재생 정책·ogg 재생·볼륨 저장).

## 외부 / 배포 대기

- [ ] 웹 배포 후 폰/데스크탑 브라우저 플레이테스트 — 페이싱·밸런스 체감 + UI/폰트/전체화면 시각 확인. (배포됨: GitHub Pages 워크플로 `deploy.yml`. **다중 push 배포 큐 경합 주의 → 몰아서 push**, `known_issues` 참고.)

## 밖에서(모바일 채팅) 구체화 — 머리 쓰는 일

- [ ] 위협별 세부 설계: 차단·폭풍은 본구현됨(기획서 §4.1). 남은 것 — 소모(갈증) 단서를 더 또렷이, 죽는 순간 연출, 위협별 흔적 대비 효과 다양화.
- [ ] 자원=수명 트레이드오프 수치 감각 (무엇을 남기면 얼마를 잃나).
- [ ] 빈 세계 회피: self-async + 유령 흔적의 구체적 작동.
