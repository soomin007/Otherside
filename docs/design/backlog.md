# Backlog — 앞으로 할 일 (다음 작업의 단일 소스)

> 합의됐으나 미착수인 작업만 남긴다. 완료·폐기 항목은 지운다(✅ 목록을 쌓지 않는다).
> 무엇을 왜 했는지는 `session_logs/` 에. **현재 상태·전체 맥락은 `00_START_HERE.md`** (새 세션은 그것부터).

## 데스크톱 (구현 — 귀한 시간)

- [ ] **온보딩 후속 (핵심 완료 — 작은 것만 남음).** 완료(2026-07-02): 문구·HUD, 시장 첫 원정 인트로+기록지, 상시 책갈피(autoload `Bookmark` — 조작 안내 재열람+원정 일대기), 원정대 이름(`ExpeditionNamer`), 첫 원정 하이라이트 오버레이 튜토리얼(autoload `Tutorial`, `controls_tutorial_seen` 게이트).
      남은 것(작음): 원정 일대기에 "도달/생환" 표시(현재 deaths 기반 죽음만), 이름 풀 확장, 오버레이 앵커 영역 실기기 미세조정, blind choice/오버레이 시각 실기기 확인.
- [ ] **글씨 흐림/깨짐 — 배포 후 재확인.** 1차 조치 적용: `default_texture_filter` Nearest(0)→Linear(1)
      (비정수 캔버스 배율에서 글리프 가장자리 계단 깨짐의 직접 원인). 배포 후 폰/시크릿 창에서 글씨가 또렷한지 확인.
      여전히 흐리면 다음 레버: 폰트 MSDF 임포트(`multichannel_signed_distance_field=true`) — 단 웹/GL Compatibility 실기기 검증 필수.
- [ ] **그래픽/비주얼 전면 교체.** 완료(2026-07-02): 공통 배경(`Backdrop`), **폰트**(본문 마루부리·타이틀 Cinzel·붓은 지도 지명 전용 `UITheme.BRUSH_FONT`), 버튼 불투명 크롬.
      **이미지 생성 프롬프트 = `docs/design/art_prompts.md`** (지도 = LOTR 손그림·조각별 아이콘 11종·잉크 reveal, 단면 6종, 초상 3종, 배경, 오프닝, 타이틀 전수). 뽑아서 절차적 draw(Map/SectionArt/Figures/Backdrop)를 TextureRect/blit 로 교체.
      **에셋 삽입 대부분 완료(2026-07-03~04):** 지도배경·아이콘 01~12, 단면 13~18, 초상 19~21, 배경 22~23, 오프닝 24~28, 타이틀 29, **아이템 30~37**, 지도 스케치 40~46 전부 삽입·배선. 웹 빌드 경량화(다운스케일 .pck 55→46MB), 데스크톱 가로판(타이틀 29·공통 23) 방향 자동 교체. 시장 초상 흰 헤일로 제거(`alpha_key` erode/white-erode/despeckle).
      남은 것(선택): **오프닝 가로판 5종(§11)·열린가방(38)** — 배선됨, 뽑으면 자동 반영. 시장 터번 실오라기 한 점(원본 디테일) 콕 지울지. 팔레트·타이포 미세 다듬기. (지도 잉크 reveal·곡선·지형지물은 **map 세션 진행 중** — 안 건드림.)
- [ ] **밸런스 튜닝 (1차 완료 — 폰 테스트 대기, 상세 `docs/design/balance_notes.md`).** end 도달 0%→~10% 확보(2026-07-02: `DESOLATION_EVERY` 16→30 + 후반 캐시 물 증량, `EDGE_LEN` 5 거리감 유지). f1 폭풍의 문 등 죽은 후반 콘텐츠 활성화, 갈증 독점(98%) 해소.
      남은 것: (1) **폰 실플레이 정성 확인**(긴장·페이싱 — 시뮬 10%는 실기기서 더 낮을 수 있음), (2) 배고픔 33% 재검토(갈증/배고픔 비율), (3) 재회 임계 `REUNION_TRACES`(8) 튜닝(end 열렸으니 가능 — 다중 원정 시뮬 필요), (4) `EDGE_LEN` 엣지별 다양화.
      도구: `balance_sim.gd`(손잡이 static var 2D 스윕)·`DebugOverlay`. 워크플로: 손잡이 → `-s balance_sim` → `core_smoke` 회귀 → 코드·기획서 동시 갱신.
- [ ] **랜드마크/선택 반영 콘텐츠 확충.** 시스템 완비(이벤트 풀 + 플래그 `sets`/`sets_persist`/`requires`, 같은 런 연쇄 + 영속).
      데이터로 더: 랜드마크별 이벤트·변형(노드 events), 같은 런 연쇄·누적 상태 스토리 분기. 일반 상황(CATALOG)은 10종 + `requires` 연쇄 2종(pool_drank·rope_spent_now) 구현됨 — 더 늘릴 여지.
- [ ] **남김 밸런스 튜닝.** "남김 한 번" 본구현 완료(런당 1회 "남기기" → 물건 1종+태그를 그 자리에 두고 *자원 -비용*만 잃고 **계속 전진**, 죽음과 분리). **자발적 죽음("여기서 끝") 제거 — 모든 죽음 비자발적(고갈/위협), 시체만.** 비용=줍기 값 대칭(물4·식3·은1·로프1), 생존 자원은 죽지 않을 만큼만. 남긴 자원=줍기 가능(uses).
      남은 것: 비용·줍기 값·uses 수치 감각(폰 테스트). 늦은 지식 전달은 게임적 허용(설계, 기획서 §3) — 토큰 안 늘림("하나만" 유지).
- [ ] **★ 지도/횡스크롤 재구조 (데스 스트랜딩식 탐험, 진행 중).** 완료: 그래프+렌더+연동, 맵 정비, **강한 미지**(가본 곳만 정체 공개,
      갈 수 있는 다음은 "?" 도착해야 드러남, 그 너머 안 보임 — `visited_nodes` 영속), **막대 걷기 폐기**, **① 이동을 맵으로 이전**(노드 클릭 →
      `Map._process` 가 마커를 보간 이동시키며 한 걸음씩 `step()`(자원 소모, 상단 HUD) → 도착/고갈사 → 그 노드 화면. 별도 이동 씬 폐기 — Expedition 은 "도착 노드 화면"이 됨),
      **④ 흔적/차단/죽음 노드화**(흔적 위치 키를 leg → `node_id` 로. 도착 카드 우선순위 = 차단 무료 통과 > 줍기 > 이벤트, `death_node_id`=도착사 목표 노드·이동 중 사 떠나온 노드, 맵에 노드별 흔적 마커 X/로프/자원점) + **횡스크롤 죽은 코드 정리**(Expedition `_draw_walker/blockage/storm` 등·StormFX 배선 제거, StormFX.gd 는 ③ 위해 보존),
      **⑤ LANDMARKS(leg) 폐기 → 노드 events 일원화**(`Situations.LANDMARKS`(255줄)+`landmark()` 제거, 변형·`sets`/`sets_persist` 콘텐츠 *전체*를 노드 events 로 이전 → 선택 반영 체인을 노드에서 복원. `crossed_blockage`/`pickup_trace` 는 ④ 의존이라 유지),
      **이동 중 자잘한 상황(맵 카드)**(`step()` 이 엣지 중 `_next_situation_leg` 마다 일반 상황(`Situations.pick`)을 띄우고 `Map` 이 카드 모달로 결정받아 이동을 잇는다 — 한 걸음마다 결정),
      **③ 지형 비주얼(양피지 고지도풍 1차)**(양피지 바탕·지형 등고선·낡은 테두리 + kind별 손그림 심볼 + 밟은 길 실선/미지 점선 + 붉은 세피아 마커. 전부 절차적 draw. 미세 조정은 "지도 비주얼 다듬기"),
      **② 도착 노드 그림 단면 탐색(TWoM) MVP**(`SectionRun`(순수 core, 예산·지점) + `SectionArt`(kind별 절차적 단면) + `Expedition` 확장. 주요 지점(events/차단/줍기)+보조 지점(node.spots) 중 조사 횟수(2)만 쓴다. event=모달·delta/empty=배너. 데모 3노드 b1·b2·c1. 단면 그림은 절차적 1차 — 나중 실제 에셋 교체 여지).
      남은 것(사용자 확정 방향): 단면 **맞춤 아트**(kind별 `SectionArt`, 트랙 B). 노드별 `spots` 는 전 9개 노드에 2개씩 배치 완료(더 늘리거나 event/threat source 다양화 여지). 엣지 길이/분기 다양화.
- [ ] **⑥ 결말 심화 (MVP 완료, 기획서 §3 결말).** MVP 완료: end 도달 시 `GameState.ending_kind()`(재회=흔적 `REUNION_TRACES`(8) 축적+무사 도달 / 순환) → `Expedition` 엔딩 패널 2종(순환=서늘·"다음 원정을 보낸다"→`next_expedition`, 재회=따뜻·"여기까지"→`go_to_title`).
      남은 것: (a) **순환의 물리적 반영** — 도달점이 다음 원정의 새 목표가 되는 맵/그래프 변화(현재는 서사 화면만, 다음 런은 같은 맵 처음부터). 설계 큼. (b) **재회 임계 밸런싱**(`REUNION_TRACES`=정서 핵심 튜닝, 폰 테스트). (c) 재회 엔딩 연출·크레딧, 재회 본 뒤 처리(현재 타이틀 복귀).
- [ ] **지도 비주얼 다듬기.** 노드 칩 스타일·팔레트, 긴 그래프 카메라/스크롤, 이름 겹침 정리. (그래픽 전면 교체와 함께)
- [ ] **폭풍 CPUParticles2D 3층 연출 + 성능 테스트.** 폭풍 메커니즘은 본구현됨(구간형+예고, 은신처/강행).
      반투명 그라데이션 띠(1층)는 `Expedition._draw_storm` 에 들어감. 남은 것: 중경 스프라이트 몇 장(2층) + 전경 CPUParticles2D 소량(3층).
      데스크톱+모바일 성능 확인, 셰이더/GPUParticles 금지.
- [ ] **설정 확장.** 설정창·데이터 초기화는 구현됨(`SettingsPanel.gd`, 타이틀 오버레이 + 확인 다이얼로그).
      남은 것: 음량은 지금 master bus 에 즉시 반영만 하고 저장 안 됨 → 세이브/복원 연결, 접근성 옵션(글자 크기 등) 추가.
- [ ] **오프닝 시퀀스 (서사·튜토리얼 대사 완료).** 서사 슬라이드쇼(`Opening.gd`, 첫 플레이 자동+스킵) + 시장 NPC 튜토리얼 대사(가방 화면 `Loadout.gd`, 첫 원정 규칙 안내) 완료. 남은 것: (1) 오프닝 문구 다듬기(사용자: 나중에), (2) 오프닝 다시보기(설정), (3) 서사·NPC 실제 삽화/연출 에셋. (조작 오버레이 튜토리얼은 완료 — autoload `Tutorial`.)
- [ ] **가방 꾸리기 + 직능 + 도구 + 무게 (MVP 완료).** 마을(`Loadout.gd`): 6칸 가방 물품 + "이번 대장의 특기"(직능) + "챙길 도구 하나"(주머니, 칸 밖) + 원정대 이름. 직능 5종(`Vocations.gd` §4.5). **도구 아이템**(`Items.gd` §4.6) — 약초·부싯돌·정화천(주머니) + 부작용 말린고기 + 위기 이벤트 5종(`Situations` weight 1). **무게**(`Items.WEIGHT` + `ExpeditionRun.WEIGHT_FREE/STEP`) — 무겁게 챙기면 걸음당 물↑.
      가방 UI(2026-07-04): 책상=삽화 타일 3열 → 탭하면 가방 슬롯으로 슬라이드(`_pick_item`), 아이템 30~37 실제 아트. **직능 트레이드오프 재조정(2026-07-04, B):** 순수 플러스 4종에 약점 부여 → 평범이 유효 선택. 시뮬 평범1.0·길잡이2.0·짐꾼1.2·물지기2.0·강골0.2·유품0.0%(기획서 §4.5·balance_notes).
      남은 것: **도구 유품 연동**(`TraceData` 확장 — 도구 남기기/줍기), 도구·위기·물품 종류 확장. **★ 밸런스 폰 튜닝(한꺼번에)** — 위기·무게·직능 트레이드오프 통합 완주율은 폰 실플레이 정성 확인·미세조정(직능 수치는 1차안). 슬라이드 애니 실기기 체감도.

## 외부 / 배포 대기

- [ ] 웹 배포 후 폰/데스크탑 브라우저 플레이테스트 — 페이싱·밸런스 체감 + 오늘까지의 UI/폰트/전체화면 시각 확인. (배포됨: GitHub Pages 워크플로 `deploy.yml`. **다중 push 배포 큐 경합 주의 → 몰아서 push**, `known_issues` 참고.)

## 밖에서(모바일 채팅) 구체화 — 머리 쓰는 일

- [ ] 위협별 세부 설계: 차단·폭풍은 본구현됨(기획서 §4.1). 남은 것 — 소모(갈증) 단서를 더 또렷이, 죽는 순간 연출, 위협별 흔적 대비 효과 다양화.
- [ ] 자원=수명 트레이드오프 수치 감각 (무엇을 남기면 얼마를 잃나).
- [ ] 빈 세계 회피: self-async + 유령 흔적의 구체적 작동.
