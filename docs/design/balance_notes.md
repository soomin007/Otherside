# 밸런스 튜닝 노트 — 이어받기용

> **이 문서 하나로 밸런스 작업을 바로 이어간다.** 사용자가 "밸런스 이어서" 하면 여기부터.
> 도구: `tests/balance_sim.gd`(정량 시뮬), `scripts/ui/DebugOverlay.gd`(실기기 세팅). 실행법은 `00_START_HERE.md` §8.
> 이력: 2026-07-01 밤 도구 제작 + 첫 발견(end 도달 0%). **2026-07-02 1차 튜닝 — end 도달 0% → ~10% 확보**(아래 §달성).

## 어떻게 작업하나 (워크플로)

1. 손잡이 하나(또는 조합)를 바꾼다. 손잡이는 `ExpeditionRun` 의 **static var**(게임 기본값 고정, 시뮬만 오버라이드).
2. `godot --headless --path . -s tests/balance_sim.gd` 실행 → 리포트 비교.
   - `[1]` 정책 비교(생존탐욕/무작위/무모) · `[2]` 시작 물 스윕 · `[3]` **2D 스윕(DESO × STEPS_PER_UNIT, static var 오버라이드→원복)** · `[4]` 콘텐츠 커버리지.
   - 볼 것: **end 도달률**, 정책별 **중앙 leg**, **평균 최종 노드 row**, **사인 분해**(갈증/배고픔), **콘텐츠 커버리지**(0=못 봄).
3. `godot --headless --path . -s tests/core_smoke.gd` 로 회귀 없는지(ALL PASS) 확인.
4. 부팅 스모크 `godot --headless --path . --quit-after 5` (에러/경고 0).
5. 수치가 코드/기획서 양쪽에 있으면 **같은 커밋에서 둘 다** 갱신(기획서 §4.3 거리 곡선 등).
6. 실기기 체감은 DebugOverlay(자원 가득/노드 점프/God)로 원하는 지점만 띄워 확인.

⚠️ **시뮬 baseline 동기화:** `balance_sim.gd`의 `BASE_START` 는 `GameState.START_RESOURCES`(=Loadout 표준 구성)와 맞춰야 한다. 시작 자원을 바꾸면 둘 다.
⚠️ **손잡이 = static var:** `ExpeditionRun` 의 `WATER_PER_STEP`/`DESOLATION_EVERY`/`FOOD_EVERY`/`GAP_*`/행렬 위험 임계(`PERIL_TOTAL`/`PERIL_WATER`/`CLOSE_CALL_AT`) 와 `MapGraph.STEPS_PER_UNIT`(엣지 걸음) 는 `const` 가 아니라 `static var`. 시뮬 `[3]`·`[8]` 이 값을 오버라이드하고 끝에 원복한다 — 게임 중엔 항상 기본값. (그래서 한 번의 실행으로 격자 스윕을 낸다.)

## 달성 (2026-07-02, 정책당 500판) — end 0% → ~10%

**사용자 결정:** 방향 = **보급 강화 (거리 유지)** / 목표 = **생존탐욕 단일 런 end 10~20%**(희소).
핵심 근거: 2D 스윕에서 `EDGE_LEN` 이 지배적 손잡이(EDGE 5 유지 시 DESO 28까지 올려도 <1%). 하지만 사용자가 **거리감(35걸음) 유지**를 택함 → `EDGE_LEN` 5 고정하고 **DESO 완화(걸음 수 불변, 후반 물 가속만 늦춤) + 후반 캐시 물 증량**으로 도달성 확보.

**적용:**
- `DESOLATION_EVERY` **16 → 30** — 후반 물 소모 누적 ~59 → ~41. `leg 30+` 에서만 2배 = 완주 마지막 ~5걸음이 가혹(거리 곡선 유지, 벽만 낮춤).
- **후반 캐시 물 증량(~+10, 초반 row 0~1 은 안 건드림 → 초반 긴장 유지):**
  · d1 뼈의 들판: `bones_gather` 물 2→3, `bones_flask` spot 물 2→3
  · e1 무너진 담: `wall_pack` spot 물 0→2(+식 2)
  · f1 폭풍의 문: `gate_relic` spot 물 2→4 ("앞선 이의 유품", 후반일수록 유품↑ 서사와 일치)
  · c2 모래의 벽: `wall_tarp` spot 물 0→2 (물 부족 경로 보강 — c1 오아시스 경로 대비 c2 폭풍 경로가 물 빈약해 절반이 여기서 죽던 비대칭 완화)
  · c1 오아시스: `oasis_edge` spot 물 2→3

**결과(EDGE 5 · DESO 30 기본값):**
- 생존탐욕 end **~10%**(중앙 28걸음) · 무모강행 **~0.2%**(스킬 격차 유지) · 무작위 0%.
- 사인 분해: **갈증 58 / 배고픔 33 / 위협 9** (이전 갈증 98% 독점 해소).
- 콘텐츠: f1 `storm_gate`·`storm_gate_eye`·`storm_gate_ill`(이전 전부 0)·후반 체인 `pool_drank→storm_gate_ill` **전부 활성화** — 죽은 후반 콘텐츠 해소(시뮬의 핵심 목적).

**여전히 0(단일 런 시뮬로는 원래 못 뜸):** `river_dug_again`·`camp_revisit_shelter`·`sand_wall_sheltered`·`bones_revisit_mourn` — 재방문 변형(영속 플래그 필요) 또는 탐욕이 안 고르는 선택이 켜는 체인. **다중 원정 시뮬(2단계, 미구현)** 영역.

## 튜닝 손잡이 (현재값 · 위치 · 효과)

| 손잡이 | 현재값 | 위치 | 효과 |
|---|---|---|---|
| 시작 자원 | 물20·식13·로프1·은1 | `GameState.gd START_RESOURCES` (+ `balance_sim BASE_START`) | 초반 여유. 크게 올리면 초반 긴장 상실 |
| 가방 물품량 | 물통+7·식+6·로프+1·은+1 (6칸) | `Loadout.gd` (책상 물품·표준 구성) | 실제 시작 자원의 진짜 소스(START_RESOURCES는 baseline) |
| 물 거리 가속 | `DESOLATION_EVERY=30` (static var) | `ExpeditionRun.gd` 손잡이 블록 | 클수록 후반 덜 가혹(도달 거리↑). **후반 도달의 핵심 손잡이** |
| 걸음당 기본 물 | `WATER_PER_STEP=1` (static var) | `ExpeditionRun.gd` | 전체 물 압박 |
| 엣지 걸음 | `MapGraph.STEPS_PER_UNIT=0.027` (static var) | `MapGraph.gd` | **곡선 경로 길이(px)×이 배율 = 엣지 걸음**(2026-07-06 거리 비례로 전환, 옛 `EDGE_LEN=5` 폐기). 가까운 엣지 2~3·먼 엣지 7~9. 지배적 손잡이 — 키우면 총 걸음↑(도달 어려움). `EDGE_MIN/MAX`(2/11)로 극단 클램프 |
| 식량 소모 주기 | `FOOD_EVERY=2` (static var) | `ExpeditionRun.gd` | 식량 압박(현재 배고픔사 33% — 이전 2%에서 상승) |
| 이동 중 상황 간격 | `GAP_MIN/MAX=2/4` (static var) | `ExpeditionRun.gd` | 결정 밀도(페이싱) |
| 초반 안전 구간 | `EARLY_SAFE_LEG=6` (static var) | `ExpeditionRun.gd` | 이 걸음 전(첫 엣지)엔 도구 위기(열병 -5 등) 억제. 초반 즉사 방지·거리 곡선(§1). 키우면 도구 가치↓ |
| 지형 개연성 배수 | `BIOME_MATCH_MULT=3` (const) | `Situations.gd` | 향하는 지형(도착 노드 biome)에 맞는 이동 이벤트 가중. 같은 풀의 분포만 기울임(새 위협·대가 없음). 키우면 개연성↑·다양성↓ |
| 남기기 비용 | 물4·식3·은1·로프1 | `ExpeditionRun.LEAVE_COST` (const) | 남김=줍기 대칭(줍기 값과 함께 봐야) |
| 재회 임계 | `REUNION_RESCUES=2` + `REUNION_MOURN=2` | `GameState.gd` | 재회 난이도(정서 핵심). 2026-07-10 세 축 개편: 기림 2(죽은 자리 표식, 영속) + 구조 2(낙오자를 거두어 이번 런에 데리고 닿기) + 온전(행렬 손실 0 — 임계는 아래 "행렬 위험 임계" 행). 옛 `REUNION_TRACES`(흔적 카운트 8→4) 축은 폐기. "공략 알면 ~4원정에 재회" 목표 유지. 주의: 기릴 자리는 죽음+유령 d1 — 한 번도 안 죽은 세계면 d1 하나뿐(실질상 없음, 폰 확인) |
| 행렬 위험 임계 | `PERIL_TOTAL=4`·`PERIL_WATER=3`·`CLOSE_CALL_AT=2` (static var) | `ExpeditionRun.gd` 행렬 블록 | 온전한 도달(재회 축 ③) 난이도 + 손실 팝업 빈도. TOTAL/WATER = 위기 판정(도구로 회피 가능), CLOSE = 바닥 스침(자원별 런당 1회). 시뮬 `[8]` 이 스윕(임계는 궤적 불변이라 같은 시드 A/B) |
| 그래프 깊이 | row 0→7 (엣지 7, 완주 35걸음) | `MapGraph.gd NODES` row | 줄이면 도달 걸음↓. "긴 여정" 느낌과 트레이드 |
| 후반 캐시 물 | 뼈 물3+flask3 / 모래벽 tarp물2 / 무너진담 pack물2 / 폭풍문 relic물4 / 오아시스 물5+edge3 | `MapGraph.gd events/spots effect` | 후반 지속력·라우팅 보상. 후반만 늘리면 초반 긴장 유지 |

## 다음 (폰 테스트 / 남은 튜닝)

1. **폰 실플레이로 정성 확인(긴장·페이싱·재미).** 시뮬 10%는 완벽 greedy 근사 — 실기기(사람)에선 더 낮을 수 있다. 체감 보고 미세조정(목표 대역 10~20% 안에서).
2. **배고픔 33% 재검토.** 갈증 독점은 풀렸으나 배고픔이 다소 높다. 취향이면 START 식량 or `FOOD_EVERY` 로 갈증/배고픔 비율 조정(예: 갈증 65/배고픔 25 목표).
3. **구조(재회 축 ②, 2026-07-10 — 옛 `REUNION_TRACES` 4 축을 교체).** 낙오자 ≥ `REUNION_RESCUES`(2)를 거두어 이번 런에 데리고 닿아야 재회. 낙오자 = 유령 씨앗(b1·c1·d2) + 과거 런의 대원 손실 자리(마을 제외). 거두기 비용 물 -2(needs 3). 데려가다 죽으면 잃는다. 교착 방지 = `_ensure_stragglers`(한 경로 2명 배치 보장, b1 우선). **시뮬 지표 없음** — 재회 경로는 다중 원정 시뮬(2단계) 몫.
4. **온전한 도달(재회 축 ③, 2026-07-10).** 행렬(대장+대원 4) 손실 0 이어야 재회. 손실 트리거(`ExpeditionRun`) = ⓐ 한 선택에 물+식량 -4 이상이며 물 -3 이상(열병/한파/폭풍/찰과 "버틴다"류·storm_gate 계열 강행이 걸림 — 도구로 대비하면 회피) ⓑ 물/식량 첫 ≤2 스침(자원별 런당 1회). 구조는 손실을 상쇄하지 않음.
   **시뮬 지표 구현됨(2026-07-11, `balance_sim` `[8]` + `_report` 온전 칼럼).** 기본 임계(4·3·2) GREEDY 500판:
   - **end 도달런 중 온전 ~26%** (전 런의 ~2%) · 평균 손실 1.77명 · **무손실 런 6%**(사실상 매 런 손실 팝업이 뜬다 — 폰에서 피로하면 CLOSE 완화 후보).
   - 원인 분해(자리 기준): 위기 354 · 갈증 스침 211 · **굶주림 스침 322**/500판 — 식량 ≤2 스침이 의외로 커서 CLOSE 는 사실상 식량 축 임계이기도 하다.
   - 스윕(같은 시드 A/B): `CLOSE 1` → end 중 온전 42%·무손실 16% / `TOTAL 5·WATER 4` → 위기 손실 반감(354→166)·end 중 32% / 셋 다 완화(5·4·1) → end 중 55%·무손실 23%. **`TOTAL 3·WATER 2` 는 기본과 동일** — 그 대역(물 -2·합 -3)에 걸리는 선택지가 콘텐츠에 없다(조이는 쪽 레버는 사실상 없음).
   - 축적 효과 확인: `[7]` 전체유령이면 온전 도달 2.0→4.2%(과거 흔적이 온전을 돕는다 — "온전 = 축적의 수확" 설계 의도와 일치).
   - **결정은 폰 체감 후(기본값 유지).** 시뮬 온전 26%는 도구를 성실히 쓰는 그리디 상한 — 사람은 더 낮을 것. 재회 런은 구조 물 -2×2 부담까지 겹쳐 실전은 더 빡빡하다(다중 원정 시뮬 몫).
5. ~~`EDGE_LEN` 다양화(엣지별 거리)~~ ✅ **2026-07-06 완료** — 엣지 걸음 = 곡선 반영 경로 길이 비례(`MapGraph.edge_steps`). 아래 §2026-07-06 참고.

### 설계 의도 (놓치지 말 것)
- end/재회는 **어렵되 가능**해야 한다. 목표는 "높은 완주율"이 아니라 **여러 원정 축적 + 좋은 런이면 닿는** 정도(단일 런 end ~10%면 충분).
- 재회는 end 도달 **+ 기림 2 + 낙오자 2 구조(이번 런에 데리고 닿기) + 온전한 도달(행렬 손실 0)** 전부 → 단일 런 시뮬의 end 도달률은 필요조건일 뿐. 정확한 재회 경로는 **다중 원정 시뮬(2단계, 미구현)**로 봐야 한다. (옛 "흔적 4개" 축은 2026-07-10 폐기 — 남기기의 가치는 온전 도달의 밑천으로 존속.)
- 거리 = 척박함의 기울기(기획서 §4.3)는 유지 — 후반이 가혹한 건 의도. DESO 30 은 벽을 낮췄을 뿐(마지막 5걸음 여전히 2배). "도달 불가"만 고쳤다.

## 다음 도구 (필요하면)
- **다중 원정 시뮬(2단계):** GameState 누적 로직(흔적·플래그·bridged) 손으로 흉내 내 재회 경로·재방문 변형 커버리지까지. 재회 임계 튜닝의 정확한 근거.

## 2026-07-04 — 직능 트레이드오프 재조정 (순수 상위호환 제거)
**문제:** 직능 4종(길잡이·물지기·강골·유품지기)이 순수 플러스라 "평범"이 지배당함(아무도 안 고름). 시뮬 확인: 평범 7%·물지기 24% 등(구 기획서 수치).
**조치(방향 B):** 각 특기에 **약점 하나** 부여 — 시작 자원 0 위험 피하려 물 커브(desolation) 위주.
- 길잡이 deso+12 + 시작물-3 · 물지기 water_gain+1 + deso-6 · 강골 food_every+2 + deso-3 · 유품지기 leave-1 + 시작물-2 · (짐꾼은 이미 트레이드오프).
**시뮬 [5] 결과:** 평범 1.0 · 길잡이 2.0 · 짐꾼 1.2 · 물지기 2.0 · 강골 0.2 · 유품지기 0.0%. 밴드 좁아짐, 순수 지배 없음.
**주의:** 갈증사 지배(80~100%)라 **end 도달률은 물 축만 반영** — 강골(배고픔 생존)·유품지기(남기기)의 가치는 이 지표 밖. 그래서 둘의 end 낮음은 정상. 최종은 폰 실플레이.

## 2026-07-08 — 콘텐츠 배치2 (arrival events +5 · 이동 중 차단/폭풍 +2 · 체인 +1) 밸런스 중립 확인
**추가:** a1 `river_weir` · b1 `camp_kettle` · d1 `bones_banner`/`bones_gust`(STORM. cache 노드에도 폭풍이 떠 장막 쓸 곳이 폭풍 노드 밖에도 생김 = 대비 자원 경쟁) · e1 `collapsed_wall_dust`(차단 변형, base 1→2) / CATALOG `rope_gully`(이동 중 차단 첫 종. 로프를 소모하지 않고 "가진 것"의 값만 만든다) · `grit_wind`(폭풍 지형 일반 이동 상황. 기존 폭풍은 도구 위기 weight 1 뿐이었음) / `dig_marks`(requires `river_dug`. a1 파기 선택의 self-async 메아리, 기존 영속 플래그의 소비처 확장).
**튜닝 과정:** 1차 수치(grit_wind 최선 식량-1 · bones_gust 웅크림 물-2 · rope_gully 맨손 -3)는 end 8.0→6.0%, 배고픔 44→51%로 식량 압박 과다. grit_wind 를 물 대가로 전환, gust -1, gully 로프 무료/맨손 -2, banner 물4로 완화 → **생존탐욕 7.4% · 갈증 47/배고픔 46**(기준선 8.0 · 48/44, 500판 노이즈 대역 안).
**커버리지:** 신규 5 도착 이벤트 전부 등장(148·83·54·66·67), 죽은 콘텐츠 0. 직능 격차·유령 영향(b2로프 12.8%)·정책 격차(무작위·무모 0%) 보존.
**교훈:** 이동 중 상황에 식량 대가를 넣으면 배고픔 사인이 빠르게 오른다(식량이 후반 병목). 이동 대가는 물 위주로, 식량 트레이드오프는 도착 이벤트에.

## 2026-07-08 — 위협 주요 지점 스킵 치즈 수정 (필수 위협) — 밸런스 무변동 확인
**문제(설계 점검):** 단면의 "떠난다"가 주요 지점 조사 여부를 게이트 안 함 → 폭풍/차단 노드에서 위협 카드를 안 열고 자원 지점만 파고 떠나면 위협 대가(폭풍 물-4·차단 물-3·f1 물-5 등)를 0원 통과. 시뮬 GREEDY 는 주요 지점을 5.0 우선이라 이 사각을 못 잡음.
**조치:** 주요 지점이 진짜 위협(폭풍/차단)이면 `mandatory` — 마주 전엔 "떠난다" 잠금(`SectionRun.has_unresolved_threat` + `Expedition._on_advance`). **필수 위협은 예산 한 칸을 미리 차지**(위협 노드 보조 탐색 −1 = 기존 정직 플레이와 같은 조사량)하되 예산과 무관하게 항상 조사 가능(소프트락 방지).
**밸런스 검증:** 생존탐욕 7.4% · DESO30 7.8% · 갈증 47/배고픔 46 — **직전 기준선과 동일**(예산 예약으로 조사량 보존). *반례:* 예산 미차감(위협 노드 +1 조사) 안으로 재보면 8.8%·11.0%·갈증 80/배고픔 11 로 완주율+사인비 크게 흔들림 → 예약 방식이 맞다.
**교훈:** 위협 필수화는 익스플로잇 제거만이 목적 — 위협 노드 조사량을 늘리면(예산 미차감) 자원(특히 식량 spot)이 딸려와 배고픔 사인이 무너진다. "필수화 = 조사량 보존"이 밸런스 중립의 핵심.

## 2026-07-06 — 엣지 걸음 = 곡선 반영 경로 길이 비례 (EDGE_LEN 폐기)
**변경:** 전 엣지 고정 5걸음 → 지도상 두 노드의 **곡선 경로 길이(px) × `STEPS_PER_UNIT`**. 가까운 노드는 적은 걸음, 먼 노드는 많은 걸음. 직선거리가 아니라 biome 곡선(river 사행·rock 볼록·storm 지그재그) 호 길이 반영.
**아키텍처:** 노드 좌표(옛 `Map.NODE_PX`)·곡선 공식을 `MapGraph`(core)로 승격 → 렌더(Map)·마커·경로 길이·시뮬이 **한 공식 공유(단일 진실)**. balance_sim(순수 core)이 실제 거리로 완주율을 검증할 수 있게 됨.
**튜닝:** 곡선이 직선보다 길어 SPU 0.031 → 완주 0%. **SPU 0.027 · DESO 30 = 생존탐욕 ~12% · 무모 0.8%**(기존 EDGE_LEN=5 완주율·스킬격차 보존). 걸음 분포: n0→a1 3 · a1→b2 2 · a1→b1 7 · … 전부 [2,11].
**손잡이 이동:** `ExpeditionRun.EDGE_LEN` 제거 → `MapGraph.STEPS_PER_UNIT`/`EDGE_MIN`/`EDGE_MAX`. core_smoke 에 `_test_edge_distance` 불변식 추가(거리 비례·범위·곡선≥직선).
