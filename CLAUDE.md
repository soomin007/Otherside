# See you on the other side — Project Context

비동기 로그라이트. 매년 원정대가 재앙을 멈추러 떠나고 거의 다 죽는다. 플레이어는 그 원정대를
거듭 보내는 **총괄자이자 매 원정의 대장 역**이다(매번 다른 사람이 간다). 모래폭풍이 글씨를 지우는 세계에서,
죽기 전 단 한 번만 다음 원정대에게 *물건*을 남길 수 있다. (제목은 남에게 하는 작별이 아니라
메타 층위의 '나'에게 하는 약속. 의미 반전이 정서의 핵. 흔적의 주체는 in-world 에선 '이전/다음 원정대'.)

- **엔진:** Godot 4.6 (GDScript). 2D. 3D 아님.
- **배포:** 웹으로 export → GitHub Pages (EoY 검증 워크플로 재활용). 개발은 데스크톱, 배포·테스트는 웹/모바일.
- **두 쌍둥이 게임 중 첫째.** 둘째와는 "비동기 로그라이트 형식"과 중심 질문만 공유, 세계·메커닉은 별개.

## 단일 진실 (Source of truth)

`docs/design/SYOTOS_기획서_v0.1.md` 가 게임 사양의 최종 권위다. 여기서 구현한다.

- 보조 사양: `docs/design/wordpool_v0.1.md` (흔적 태그 단어 풀, 총 27단어, 유한·큐레이션).
- **동작이 불명확하거나 미정이면 멈추고 물어본다 — 규칙을 임의로 지어내지 않는다.** (재앙의 정체,
  적대 존재 등은 의도적으로 비워둔 맥거핀. 채워야 할 것 같아도 먼저 확인.)
- 구현 중 규칙이 바뀌면 **같은 변경에서 기획서도 갱신**한다. 코드와 사양이 말없이 갈라지면 안 된다.
- 코드로 옮긴 사양 데이터(`Threats.gd`, `WordPool.gd`)는 기획서를 *복사*한 것 — 기획서가 바뀌면 같이 바꾼다.

## 아키텍처 — 타협 불가

게임을 두 층으로 나누고, **서로 스며들지 않게** 유지한다:

- `scripts/core/` — **순수 데이터·로직.** 노드 트리·렌더링에 의존하지 않는 `RefCounted`/데이터 클래스.
  결정론적이고 단위 테스트 가능. 흔적·위협·자원·세이브 모델이 여기 산다. 이식 가능한 자산으로 취급.
- `scripts/ui/` + `scenes/` — 렌더링·입력. core 에 의존하되, **core 는 ui 를 절대 import 하지 않는다.**
- `scripts/GameState.gd` — autoload 싱글톤. 전역 상태 + 씬 라우팅 + 저장/불러오기. core 데이터를 들고 ui 가 읽는다.

core 가 노드 트리를 만지거나 ui 를 참조하면 그건 버그다.

### 두 레이어 (게임 구조)
코어 루프가 두 화면으로 돈다 (기획서 §3, §5):
- **탑뷰 지도 (`scenes/map.tscn`)** — 계획. 누적된 길 / 죽은 자리 / 남긴 흔적의 점. 안개가 원정마다 걷힌다.
- **횡스크롤 단면 (`scenes/expedition.tscn`)** — 실행. 한 걸음씩 전진, 자원 소모, 위협, 흔적 판독.
- 루프: **지도(계획) → 횡스크롤(실행) → 죽음 → [남김 한 번] → 지도 갱신 → 다음 원정.**
- 핵심 동사는 액션이 아니라 **관리·판독·대비·남기기** (턴제 사색형). 페이싱("한 걸음마다 결정할 게 있나")이 생명.

## 웹 export 제약 (반드시 지킬 것)

- **셰이더 / GPUParticles 금지** — 웹 export 에서 깨짐 (EoY 에서 확인). 폭풍은 **CPUParticles2D**.
- **폭풍 = 3층 레이어**로 경량화: ①반투명 그라데이션(분위기) ②중속 스프라이트 몇 장(중경) ③진짜 파티클 소량(전경).
- 세이브 = `user://` (웹에선 브라우저 IndexedDB 에 영속). `GameState` 가 JSON 으로 직렬화 — 상태는 JSON 직렬화 가능하게 유지.
- 렌더러 = **GL Compatibility** (web/mobile). 첫 로딩이 느릴 수 있으니 에셋 경량 유지.
- export 프리셋 "Web" 은 **Threads Support 끄기** + COOP/COEP 헤더 ON (`export_presets.cfg` 에 반영됨).

## 모바일 / 터치 우선

- 웹 빌드라 폰 브라우저에서 그대로 플레이테스트 가능 → 밖에서 밸런스 체감.
- **터치 우선 UI를 처음부터 제약으로.** 버튼은 손가락 기준 큼직하게(최소 ~56px), 간격 넉넉히. 턴제라 탭 위주.
- 폭풍 파티클 수는 폰에서 확 줄이고 배경 레이어로 메운다.

## GDScript 규칙

전역 규칙은 `~/.claude/CLAUDE.md` (Godot 4.6 컨벤션)을 따른다. 핵심만:
- 예약어(`signal`/`func`/`class`)를 변수·파라미터명으로 쓰지 않는다.
- 모든 변수·반환값에 타입 힌팅. untyped Array 를 인덱싱해 꺼낼 때 `:=` 금지, 명시적 타입 선언.
- `Array[T]` 에 untyped Array(사전 값, `Dictionary.get` 결과 등)를 직접 대입하지 않는다 — `for` 루프로 요소별 `append`.

## UI 문구 규칙 (한국어, 사용자에게 보이는 모든 멘트)

- **한글 사이 em dash(—) 금지.** 마침표·쉼표·줄바꿈으로 대신한다.
- 쉬운 말. 태그 단어 풀은 기획 어휘 그대로 쓰되(`또`·`봐` 등), 시스템 안내문은 풀어 쓴다.

## 명령어 (데스크톱)

- 에디터 열기: `Godot_v4.6.1-stable_win64.exe --path .` (또는 `godot --path .`)
- 파싱·임포트 검증(헤드리스): `godot --headless --path . --import`
- 부팅 스모크 테스트: `godot --headless --path . --quit-after 5` (에러/경고 0 이어야 함)
- 웹 export(로컬): `godot --headless --path . --export-release "Web" build/web/index.html`

## 운영 규칙 (세션 루틴 · 커밋 · 문서)

### 세션 시작 루틴 (본격 작업 전 가장 먼저)
다음을 읽고 "직전까지 한 것 / 다음 후보 / 추천 한 가지"를 정리해 제안한 뒤 시작한다:
0. **`ACTIVE_WORK.md` (작업 조율판) — 가장 먼저.** 다른 세션이 claim 한 범위를 피해 내 블록을 추가한 뒤 시작.
1. `docs/design/backlog.md` — 다음 작업의 단일 소스
2. 최신 `session_logs/YYYY-MM-DD.md` — 직전 세션 상태·결정·미해결
3. `docs/design/known_issues.md` — 반복 금지 함정/오류
4. `git status` + `git log --oneline -10`

### Git push 루틴
의미 있는 작업 단위(기능·시스템 변경·문서 대량 갱신·세션 로그)가 끝나면 **즉시 commit + push origin main**.
작은 단위로 자주 끊는다.
- 커밋 메시지: 한국어, `prefix(scope): 설명` (feat/fix/refactor/docs/chore), 이모지 없음, 본문은 변경 항목 bullet.
- `main` 에 직접 push (branch/PR 안 씀). destructive 작업(force push, reset --hard)은 먼저 확인.
- **`git add -A`/`git add .` 금지** — 내가 바꾼 파일만 경로로 명시 스테이징(동시 세션 보호). 커밋 전 `git status` 확인.
- ⚠️ **아직 GitHub 원격·Pages 미설정** (외부 작업). 원격이 붙기 전까진 로컬 커밋만. 설정은 사용자 몫.

### 동시 세션 루틴 (여러 터미널 병행 시)
`ACTIVE_WORK.md` 가 겹침 방지의 단일 진실 (git 미추적·로컬 실시간 공유). 시작 전 읽고, 내 블록만 편집,
끝나면 내 블록 삭제. 공유 문서(`session_logs/`·`backlog.md`·`known_issues.md`)는 끝에 append 만.

### 오류 기록 루틴 (반복 방지)
버그·설계 함정·작업 실수를 발견하면 `docs/design/known_issues.md` 에 "증상 → 원인 → 재발 방지책"으로 기록.
게임 버그뿐 아니라 프로세스 실수(도구 오용, 커밋 누락 등)도 포함.

### 세션 로그
매 세션 종료 시 `session_logs/YYYY-MM-DD.md` 에 요약·주요 변경·결정·미해결 기록. 같은 날짜면 이어서 추가.
(전역 `~/.claude/CLAUDE.md` 의 세션 로그 루틴을 따른다.)

### 구현 계획 문서는 "앞으로 할 일"만 남긴다
`docs/design/backlog.md` 에는 앞으로 할 일만. 완료·폐기된 항목은 지운다(✅ 달성 목록을 쌓지 않는다).
무엇을 왜 했는지는 `session_logs/` (+ 필요 시 `known_issues.md`)에 남긴다.

## Scope

In scope (Phase 0 → 구현 진입): 코어 루프 뼈대(지도↔횡스크롤↔죽음↔흔적), 횡스크롤 전진+자원 소모
프로토타입, 폭풍 CPUParticles2D 3층 성능 테스트, "남김 한 번 → 다음 런 흔적 줍기" 최소 구현, 웹 export 파이프라인.

Out of scope (당분간): 진짜 다른 플레이어와의 비동기 공유(서버 필요 — 1차는 self-async + 손으로 심은 유령
흔적), 전투 풀스택(적대 존재는 맥거핀, 넣더라도 회피 대상), 흔적 클로즈업 포인트앤클릭(여유 생기면). 지시 없으면 만들지 않는다.
